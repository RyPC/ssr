# iOS Integration

Planned stack: Swift, AVFoundation (camera capture), Vision framework (face landmarks),
CoreML (exported sequence model).

Not yet implemented — depends on a trained model export from `ssr/models`.
