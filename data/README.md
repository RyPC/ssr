# Data

- `raw/` — self-recorded phrase clips (Phase 1), gitignored
- `processed/` — extracted landmarks/features, gitignored
- Public datasets (GRID, LRS2/LRS3) are downloaded separately; see `ssr/data/datasets.py`.
